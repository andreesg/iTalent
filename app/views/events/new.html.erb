
<%= form_for(@event) do |e| %>
      <%= e.label :title %>
      <%= e.text_field :title %>
      
      <%= e.label :description %>
      <%= e.text_field :description %>
      
      <%= e.submit "Create event", class: "btn btn-large btn-primary" %>
<% end %>